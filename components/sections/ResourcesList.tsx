import React from "react";
import SectionWithGrid from "./SectionWithGrid";

type Props = {};

const ResourcesList = (props: Props) => {
  return (
    <SectionWithGrid id="article-list" title="Other Resources">
      hi
    </SectionWithGrid>
  );
};

export default ResourcesList;
